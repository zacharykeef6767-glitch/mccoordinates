# ⛏ MC Coord Tracker — Mobile App

## 📲 Install on your phone in 2 steps

---

### STEP 1 — Host the app (free, 30 seconds)

1. Go to **netlify.com/drop** on any computer
2. Drag this entire `mc-coord-pwa` folder onto the page
3. Netlify gives you a free URL like `https://amazing-name-123.netlify.app`

That's it. Share that link with anyone.

---

### STEP 2 — Install on phone

**Android (Chrome):**
- Open the URL in Chrome
- Tap the ⋮ menu → "Add to Home screen"
- OR tap the **INSTALL** banner that pops up at the top

**iPhone / iPad (Safari):**
- Open the URL in Safari
- Tap the **Share** button (box with arrow) → "Add to Home Screen"
- Tap "Add" — done!

The app will appear on your home screen like any other app and works offline.

---

## Files in this package

```
mc-coord-pwa/
├── index.html      ← The entire app
├── manifest.json   ← Makes it installable
├── sw.js           ← Enables offline mode
├── icon-192.png    ← App icon
└── icon-512.png    ← App icon (large)
```

## Features

- 🌍 World / Server profiles with color tags
- 📍 Save coords with dimension, category & notes
- 🔍 Search and filter
- 🔥 Nether ↔ Overworld calculator
- 📤 Export all data as JSON
- 💾 Works fully offline after first load
- 📲 Installs to home screen like a native app
